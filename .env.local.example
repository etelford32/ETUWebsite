# ==============================================================================
# Supabase Configuration (NEW API Key Format)
# ==============================================================================
#
# ⚠️  IMPORTANT: Supabase has deprecated the old anon_key/service_role_key system
# We now use Publishable Keys and Secret Keys
#
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
# ==============================================================================

# Supabase Project URL
NEXT_PUBLIC_SUPABASE_URL=https://osvrbwvxnbpwsmgvdmkm.supabase.co

# Publishable Key (safe for browser use - starts with sb_publishable_)
# This key is safe to expose in the browser ONLY if Row Level Security (RLS) is enabled
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=sb_publishable_YOUR_KEY_HERE

# Secret Key (server-side only - starts with sb_secret_)
# ⚠️  CRITICAL: NEVER expose this key in client-side code or commit to Git!
# This key bypasses RLS and has full database access
# Only use in API routes and server components
SUPABASE_SECRET_KEY=sb_secret_YOUR_KEY_HERE

# ==============================================================================
# Steam API Configuration (Optional)
# ==============================================================================
# Get from: https://steamcommunity.com/dev/apikey
STEAM_WEB_API_KEY=your_steam_api_key_here

# ==============================================================================
# Site Configuration
# ==============================================================================
NEXT_PUBLIC_SITE_URL=https://exploretheuniverse2175.com

# ==============================================================================
# Local Development
# ==============================================================================
# For local development, copy this file to .env.local and fill in your actual values
# .env.local is gitignored and will not be committed to version control
#
# Steps:
# 1. cp .env.local.example .env.local
# 2. Fill in your actual Supabase keys from the dashboard
# 3. Never commit .env.local to Git
# ==============================================================================

# ==============================================================================
# Security Checklist
# ==============================================================================
# Before deploying to production:
#
# ✅ Row Level Security (RLS) enabled on ALL database tables
# ✅ RLS policies configured for each table
# ✅ Secret key is NEVER exposed to browser (no NEXT_PUBLIC_ prefix)
# ✅ Publishable key is used in client-side code
# ✅ All environment variables added to Vercel
# ✅ Tested full auth flow (signup, login, logout)
# ✅ Tested database access with RLS policies
#
# See ALPHA_SECURITY_CHECKLIST.md for complete security verification
# ==============================================================================
