# Environment Variables Configuration
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control!

# ====================
# REQUIRED - SUPABASE
# ====================
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api

# Supabase Project URL (server-only, NOT exposed to browser)
SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Service Role Key (server-only, KEEP SECRET!)
# This key has full database access - NEVER expose to browser!
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ====================
# REQUIRED - SESSION
# ====================
# Generate a secure random string (min 32 characters)
# Use: openssl rand -base64 32
SESSION_SECRET=generate-a-random-secret-here-min-32-chars

# ====================
# OPTIONAL - STEAM API
# ====================
# Get from: https://steamcommunity.com/dev/apikey
# Required for Steam login integration
STEAM_WEB_API_KEY=your_steam_api_key_here

# ====================
# OPTIONAL - EMAIL (RESEND)
# ====================
# Get from: https://resend.com/api-keys
# Required for magic link emails and notifications
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ====================
# OPTIONAL - DISCORD
# ====================
# Discord webhook URL for notifications (alpha applications, etc.)
# Get from: Discord Server Settings > Integrations > Webhooks
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/xxx/xxx

# ====================
# OPTIONAL - SITE URL
# ====================
# Your site URL for redirects and OAuth callbacks
# Development: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ====================
# OPTIONAL - CRON JOBS
# ====================
# Secret for authenticating cron job requests
# Generate with: openssl rand -hex 32
CRON_SECRET=your_cron_secret_here

# ====================
# IMPORTANT SECURITY NOTES
# ====================
# 1. NEVER use NEXT_PUBLIC_ prefix for sensitive keys!
#    - NEXT_PUBLIC_ variables are exposed to the browser
#    - Only use for truly public values (like site URL)
#
# 2. Always use server-side environment variables for:
#    - Database credentials (SUPABASE_*)
#    - API keys (STEAM_WEB_API_KEY)
#    - Session secrets (SESSION_SECRET)
#    - Admin credentials
#
# 3. Rotate keys regularly:
#    - Rotate SUPABASE_SERVICE_ROLE_KEY if exposed
#    - Rotate SESSION_SECRET periodically
#    - Rotate API keys if compromised
#
# 4. In production (Vercel):
#    - Add all variables in Vercel Dashboard
#    - Select: Production, Preview, Development
#    - Never commit secrets to Git
